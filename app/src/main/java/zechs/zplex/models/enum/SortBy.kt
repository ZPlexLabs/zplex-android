package zechs.zplex.models.enum


enum class SortBy {
    popularity,
    release_date,
    revenue,
    primary_release_date,
    original_title,
    vote_average,
    vote_count
}

enum class Order {
    asc, desc
}

